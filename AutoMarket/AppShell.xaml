<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AutoMarket"
    x:Class="AutoMarket.AppShell"
    
    Shell.TabBarBackgroundColor="#FFFFFF"
    Shell.TabBarTitleColor="#1A1A1A"
    Shell.TabBarUnselectedColor="#6B6B6B"
    Shell.TabBarDisabledColor="#FF3333"
    Shell.TabBarForegroundColor="#1A1A1A">
    
    <TabBar>

        <Tab Title="Головна" Icon="home.svg">
            <ShellContent
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="MainPage" />
        </Tab>

        <Tab Title="Обране" Icon="heart.svg">
            <ShellContent
                ContentTemplate="{DataTemplate local:FavoritesPage}"
                Route="FavoritesPage" />
        </Tab>

        <Tab Title="Продати" Icon="plus_circle.png">
            <ShellContent
                ContentTemplate="{DataTemplate local:SellPage}"
                Route="SellPage" />
        </Tab>

        <Tab Title="Чат" Icon="chat.svg">
            <ShellContent
                ContentTemplate="{DataTemplate local:ChatPage}"
                Route="ChatPage" />
        </Tab>

        <Tab Title="Профіль" Icon="menu.svg">
            <ShellContent
                ContentTemplate="{DataTemplate local:ProfilePage}"
                Route="ProfilePage" />
        </Tab>

    </TabBar>

</Shell>